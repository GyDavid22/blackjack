{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */

    /* Modules */
    "rootDir": "./blackjack",                            /* Specify the root folder within your source files. */
    "outFile": "./blackjack.js",                         /* Specify a file that bundles all outputs into one JavaScript file. If 'declaration' is true, also designates a file that bundles all .d.ts output. */

    /* Type Checking */
    "strict": true,                                      /* Enable all strict type-checking options. */
  }
}
